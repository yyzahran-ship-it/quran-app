package com.quranapp.audio

import org.junit.jupiter.api.Test

/**
 * AudioPlaybackService extends MediaSessionService which requires an Android runtime.
 * Integration tests live in androidTest/; this stub satisfies the test source set.
 */
class AudioPlaybackServiceTest {

    @Test
    fun `placeholder - service tests run as instrumented tests`() {
        // No-op: real service tests are in src/androidTest
    }
}
