import Flutter
import UIKit
import XCTest

@testable import Runner

class RunnerTests: XCTestCase {
    func testExample() throws {
        // Flutter unit tests are run via `flutter test` — nothing needed here.
    }
}
