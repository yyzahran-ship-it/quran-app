# Requirements for scripts/align_reciters.py
#
# Install: pip install -r scripts/requirements_align.txt
#
# For aeneas backend you also need system packages:
#   sudo apt-get install ffmpeg espeak espeak-data libespeak-dev python3-dev
#
# For whisperx backend (default), ffmpeg must be on PATH:
#   sudo apt-get install ffmpeg   (or brew install ffmpeg on macOS)

requests>=2.31.0
pyarabic>=0.6.14
whisperx>=3.1.1
torch>=2.0.0
torchaudio>=2.0.0
# Faster CPU inference (optional, whisperx will use it automatically):
faster-whisper>=0.9.0

# For --tool aeneas backend:
aeneas>=1.7.3
srt>=3.5.2
